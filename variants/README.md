# variants/

One workspace per rendering paradigm — vanilla, heavy-hydration (React/Next),
islands (Astro), resumability (Qwik), hypermedia (HTMX), plus the fenced Remix 3
frontier (ADR-0004 §2).

Empty in this slice. The throwaway placeholder stand-in variants (one static, one
SSR) arrive with the composed-origin slice (issue #3); real paradigm variants are
downstream per-surface builds.
