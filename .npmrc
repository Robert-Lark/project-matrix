# Public registry, pinned: this repo is public and reproducible-by-anyone
# (ADR-0001 §9) — installs must not depend on an org-internal mirror.
# (pnpm-specific settings — e.g. the hoist policy — live in
# pnpm-workspace.yaml, where pnpm ≥10 reads them.)
registry=https://registry.npmjs.org/
