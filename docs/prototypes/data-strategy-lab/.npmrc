# Same rationale as the repo root's .npmrc: this repo is public and
# reproducible-by-anyone (ADR-0001 §9) — installs must not depend on an
# org-internal mirror. The prototype is its own npm project root, so the
# root pin does not reach it; pin here too.
registry=https://registry.npmjs.org/
