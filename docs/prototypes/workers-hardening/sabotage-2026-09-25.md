# Sabotage tables — workers hardening (unit 8), 2026-09-25

One deliberate defect per row; the owning guard must fail; CONTROL rows must
pass. Backups are taken FRESH per row and every restore is verified by byte
equality against its backup (unit 7's stale-backup lesson); guards run under
`bash -c` (unit 7: `bash -lc` handed node to a version-manager shim and every
row read CAUGHT with exit 126 while nothing had run). Verdicts come from the
runner's exit codes (`sabotage.mjs`, beside this file), never by eye. Run
with nothing else on the tree — no suite, no held plane (row B3b rebuilds the
front dist, which would reload a held front Worker).

## Round 1 — before verify-slice, over every new guard

Guards: `V` = `@pm/front` test/publish-refusals.test.js; `TC(w)` = that
Worker's `tsc --noEmit`; the edge and blog unit suites; the bench-runner
schema leg.

| row | defect | result | exit |
|---|---|---|---|
| A0 | CONTROL: no defect — the refusal suite passes | control — PASSED as designed | 0 |
| A1 | publish.mjs: dirty-tree refusal INVERTED (a clean tree is refused, a dirty one admitted) | CAUGHT | 1 |
| A2 | publish.mjs: cross-tree refusal removed | CAUGHT | 1 |
| A3 | publish.mjs: constant-spread comparison flipped (> becomes <) | CAUGHT | 1 |
| A4 | publish.mjs: batch-integrity SHA check removed | CAUGHT | 1 |
| A5 | publish.mjs: chrome-constant identity gate INVERTED | CAUGHT | 1 |
| A6 | publish.mjs: unattested-origin (originCommit: null) refusal removed | CAUGHT | 1 |
| A7 | publish.mjs: a NEW refusal added with no fixture row (non-vacuity: throw-site count) | CAUGHT | 1 |
| A8 | a committed fixture deleted from disk (receipts/dirty-tree.json) | CAUGHT | 1 |
| A9 | a committed fixture edited by hand (valid.json: runsPerUrl 3 → 4) | CAUGHT | 1 |
| A10 | generate.mjs: the cross-tree row deleted from CASES (file left behind) | CAUGHT | 1 |
| A11 | build.mjs: the composer no longer calls assertBatchIntegrity (the gate bypassed by its caller) | CAUGHT | 1 |
| A12 | generate.mjs: a fixture row's mutation made a no-op (dirty-tree sets dirty=false) | CAUGHT | 1 |
| B0 | CONTROL: all three Worker typechecks green | control — PASSED as designed | 0 |
| B1 | db.js: savePost's result-union field renamed (updated_at → updatedAt) — the prompt's Done-means typo | CAUGHT | 1 |
| B2 | edge index.js: a binding name typo (env.WARM → env.WARN) | CAUGHT | 1 |
| B3 | front: generated/ deleted, tsc run WITHOUT turbo (the dependsOn is load-bearing: ts(2307)) | CAUGHT | 1 |
| B3b | CONTROL: front: generated/ deleted, typecheck run THROUGH turbo (build runs first) | control — PASSED as designed | 0 |
| B4 | fit.d.mts: interactionId removed again (the drift the unit found) | CAUGHT | 1 |
| B5 | fit.mjs: FIT.pdp gains a field the declaration lacks (interactionFetchh) | CAUGHT | 1 |
| B6 | edge: a NEW src file with an implicit-any parameter and no pragma (checkJs is tsconfig-wide) | CAUGHT | 1 |
| B7 | blog editor main.js: a dialog method called on a plain element (mediaDialog cast removed) | CAUGHT | 1 |
| C0 | CONTROL: edge + blog unit suites pass | control — PASSED as designed | 0 |
| C1 | edge: the beacon value check removed, the old 0-coercion restored | CAUGHT | 1 |
| C2 | blog html.js: esc() drops the single quote again | CAUGHT | 1 |
| E0 | CONTROL: the bench-runner schema leg passes | control — PASSED as designed | 0 |
| E1 | valid.json loses a field the runner's schema requires (runLocation) | CAUGHT | 1 |

Row E2 (an INVENTED field on valid.json — expected MISSED, because Zod strips
unknown keys) did not run in this pass: its edit anchor assumed the run
object printed on one line and the generator prints runs across lines, so
the runner threw before the row and after the restore. It ran in round 2
with the anchor fixed; its expected verdict was stated before it ran.

## Round 2 — over the verify-slice fixes: 19 rows, 15 caught, 1 control, 3 missed

Run after the four lenses returned (the first commit of this unit, #48, had
already merged; this round is the completing commit's). Verdicts stated
BEFORE the run for the two rows expected to miss: R16 (a stale local copy
of the reading-table names in the origin suite — the local typecheck cannot
see a list the suite no longer imports; the suite's own leg on a plane is
the catcher) and E2 (Zod strips unknown keys, so an invented field passes
the schema leg — the real-receipt control and the gate's typedef hold field
names). R15 MISSED for the row's own reason: `renderChrome` already returns
the bare fragment, so swapping `chromeFragmentOf` for identity changed
nothing; R15b plants a real defect (the control hashes an UNPOPULATED
render while its constant claims populated) and the identity gate refuses
the control's own constant — CAUGHT.

| row | defect | result | exit |
|---|---|---|---|
| R0 | CONTROL (round 2): the refusal suite, the edge suite and the three typechecks pass on the fixed tree | control — PASSED as designed | 0 |
| R1 | edge: isFinite half of the value check dropped (typeof only) — the raw 1e999 legs must catch it | CAUGHT | 1 |
| R2 | publish.mjs: one `// refusal:` marker removed (a throw with no row binding) | CAUGHT | 1 |
| R3 | publish.mjs: a marker relabelled to a name no row carries | CAUGHT | 1 |
| R4 | front index.js: a sibling binding misspelt (BLOG → BLGO) — typed to Env now | CAUGHT | 1 |
| R5 | publish.mjs: a reading-table row named outside the switcher's union ("INP" for "INP (scripted)") | CAUGHT | 1 |
| R6 | publish.mjs: cc provenance drops the dirty-origin clause (skeptic M1) | CAUGHT | 1 |
| R7 | publish.mjs: tolerance finiteness dropped (skeptic M5 — Infinity would never fire the constancy check) | CAUGHT | 1 |
| R8 | publish.mjs: band overlap `>=` → `>` (skeptic M15 — touching bands would publish a ranking) | CAUGHT | 1 |
| R9 | publish.mjs: provenance cutoff `>=` → `>` (skeptic M17 — the cutoff day itself skips attestation) | CAUGHT | 1 |
| R10 | publish.mjs: column axis exact → subset (skeptic M20 — a fifth variant admitted) | CAUGHT | 1 |
| R11 | publish.mjs: unsettled `!== true` → `=== false` (skeptic M23 — an ABSENT flag admits) | CAUGHT | 1 |
| R12 | publish.mjs: populated `!== true` → `=== false` (skeptic M26) | CAUGHT | 1 |
| R13 | publish.mjs: longTaskMs dropped from the finite-delta metric list (skeptic M27) | CAUGHT | 1 |
| R14 | publish.mjs: encoding token no longer lower-cased — the upper-case CONTROL leg must catch the false refusal | CAUGHT | 1 |
| R15 | publish-refusals: the real-renderer chrome-constant control — chromeFragmentOf swapped for identity (a constant hashed from the whole page, not the fragment) | MISSED | 0 |
| R15b | publish-refusals: the real-renderer control hashes an UNPOPULATED render (lab: undefined) while the constant claims populated — the identity gate must refuse the control's own constant | CAUGHT | 1 |
| R16 | published-readings.test.ts: the switcher's READING_METRICS import replaced by a stale local copy ("INP") — the typecheck cannot see it; MISSED expected on the local guard, the suite's own leg is the catcher on a plane | MISSED | 0 |
| E2 | valid.json gains an INVENTED field (interactionSettledd) — Zod strips unknown keys: expected MISSED, stated | MISSED | 0 |

