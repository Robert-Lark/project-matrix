# Sabotage tables — decision-map compaction (unit 9), 2026-09-25

One deliberate defect per row; the owning guard must fail; CONTROL rows must
pass. The runner (`sabotage.mjs`, beside this file) is unit 8's, re-rowed:
backups are taken FRESH per row and every restore is verified by byte
equality against its backup; guards run under `bash -c`; the narrative a row
pastes back into a node is read out of `docs/build-log.md`'s own
"narrative moved from the decision map" sub-entry, so the runner depends on
nothing outside the repo. Verdicts come from the runner's exit codes, never
by eye. A row whose expected verdict is MISSED says so in its defect text
before it runs. Run with nothing else on the tree — no suite, no held plane:

    SCRATCH=<dir> node docs/prototypes/decision-map-compaction/sabotage.mjs [rowId…]

## Round 1 — over every new guard, on the compacted tree

Guards: the N/H/P rows run `tools/repo-checks/test/decision-map-node-cap.test.ts`
(the whole file, or one leg by `-t`); the L rows run
`how-built-links-resolve.test.ts`'s line-anchor leg alone (its ceiling leg is
red on this tree by design, see the entry); the R rows run `@pm/reference`'s
test file (the master regeneration pin). Cap read from the test: 7,168 B.

| row | defect | result | exit |
|---|---|---|---|
| N0 | CONTROL: no defect — the decision-map leg passes on the compacted tree | control — PASSED as designed | 0 |
| N1 | the plp-htmx narrative pasted back into its RESOLVED node (the prompt's own Done-means sabotage) | CAUGHT | 1 |
| N2 | a smaller paste: 1 KiB over the cap into security-floor (the cap is a line, not a cliff) | CAUGHT | 1 |
| N3 | CONTROL: an OPEN node (domain-cutover) padded 8 KiB past the cap — exempt by design, the leg must PASS | control — PASSED as designed | 0 |
| N4 | the N1 paste PLUS the node's Status flipped to `open` — the exemption trusts the Status line; expected MISSED, stated limit | MISSED — as stated in advance | 0 |
| N5 | every Status line flipped to `open` (the exemption swallows the rule): the minority leg | CAUGHT | 1 |
| N6 | every `### ` heading demoted to `#### ` (zero nodes): the non-vacuity floor | CAUGHT | 1 |
| N7 | security-floor loses its Status line (a node that names no status is neither open nor resolved) | CAUGHT | 1 |
| N8 | a NEW resolved node appended, one byte over the cap | CAUGHT | 1 |
| H1 | the header's stale size figure re-inserted ("at ~182 KB") | CAUGHT | 1 |
| H2 | the header's size command removed | CAUGHT | 1 |
| P1 | the plp-htmx moved-narrative sub-entry's heading reworded in the log (the map's pointer now dangles) | CAUGHT | 1 |
| P2 | a compacted node's Status quotes a phase entry title that does not exist | CAUGHT | 1 |
| L0 | CONTROL: the how-built line anchors resolve on the regenerated master | control — PASSED as designed | 0 |
| L1 | one line inserted above `## Phase 15` in the log WITHOUT regenerating the master (the line anchors shift) | CAUGHT | 1 |
| R0 | CONTROL: @pm/reference's master regeneration test passes (the how-it-was-built index is pinned) | control — PASSED as designed | 0 |
| R1 | the how-it-was-built master's phase-15 line anchor edited by hand (index and master disagree) | CAUGHT | 1 |

N4 is the leg's stated limit, recorded rather than hidden: the open-node
exemption trusts the `Status:` line the way every registry guard in this
repo trusts its registry, so a resolved node relabelled `open` with its
narrative pasted back passes. N5 is the other half of that limit — relabel
EVERY node and the minority leg fires. Rows N1, N2 and N8 each failed on the
cap leg naming the node and its byte count; H1/H2 on the header leg; P1/P2
on the pointer leg; L1 on the line-anchor leg (`?plain=1#L4508` no longer
carried `## Phase 15 — …`); R1 on the regeneration pin.

## Round 2 — over the verify-slice fixes (correctness lens, 2026-09-26)

The lens found the status classifier a guess (every in-flight phrasing the
map's history holds — `PRD published`, `sliced`, `SPEC WRITTEN, NOT BUILT`,
`CODE COMPLETE, NOT MERGED` — read as RESOLVED), the pointer leg a substring
match (`"Phase 15"` passed; another node's sub-entry passed), the header
regex blind to `bytes` and `KiB` while false-failing `B`, and the last
node's span one byte long. All four are fixed in the test; these rows prove
the fixes bite and that the exemption still holds for active work.

| row | defect | result | exit |
|---|---|---|---|
| N0 | CONTROL: no defect — the decision-map leg passes on the compacted tree | control — PASSED as designed | 0 |
| V1 | security-floor's verdict replaced by a word nobody classified ("wrapped up"): the vocabulary leg | CAUGHT | 1 |
| V2 | CONTROL: security-floor relabelled an IN-FLIGHT verdict (CODE COMPLETE, NOT MERGED) and padded 8 KiB past the cap — active work is exempt, the leg must PASS | control — PASSED as designed | 0 |
| P3 | plp-htmx's moved-narrative pointer renamed to plp-react-next's sub-entry (a real heading, the wrong node) | CAUGHT | 1 |
| P4 | security-floor's Status quotes the fragment "Phase 15" in place of the entry title (a substring of a real heading) | CAUGHT | 1 |
| H3 | the header's stale size figure re-inserted in the unit the regex used to miss ("at 253,385 bytes") | CAUGHT | 1 |
| S1 | the LAST node padded to exactly one byte over the cap — the last-node span is measured without the file's final newline (this row's first text named `a11y-section`; the last node is `workers-hardening`, and round 4's runner derives the name and asserts the node is RESOLVED) | CAUGHT | 1 |
| S1b | CONTROL: the LAST node padded to EXACTLY the cap — must PASS (an off-by-one would fail it) | control — PASSED as designed | 0 |

V2 and S1b are the controls that matter: an in-flight verdict padded 8 KiB
past the cap passes (active work is exempt, by the vocabulary rather than by
two words), and the last node padded to EXACTLY the cap passes while one
byte more (S1) fails — the off-by-one the lens measured is gone.

## Round 3 — over the conformance lens's fix (2026-09-26)

The conformance lens proved the pointer leg vacuous for a node that quotes
nothing: with every map→log pointer deleted and all sixteen sub-entries cut
from the log, every leg stayed green. The leg now holds the two sets equal
in both directions and non-empty (the OWED completeness shape).

| row | defect | result | exit |
|---|---|---|---|
| N0 | CONTROL: no defect — the decision-map leg passes on the compacted tree | control — PASSED as designed | 0 |
| P5 | security-floor's Status loses its moved-narrative pointer clause entirely (a node that quotes nothing passed the old leg) | CAUGHT | 1 |
| P6 | security-floor's moved-narrative sub-entry heading deleted from the log (its paragraphs left behind, orphaned under the previous entry) | CAUGHT | 1 |
| P7 | every moved-narrative pointer deleted from every Status AND every sub-entry heading deleted from the log (the lens's mutation: the old file stayed green) | CAUGHT | 1 |

## Round 4 — over the skeptic lens's fixes (2026-09-26)

The skeptic lens found the preamble unmeasured (20 KB pasted under
`## Notes` passed every leg), the both-directions leg satisfied by a bare
unquoted phrase naming another node, and rows S1/S1b naming the wrong node.
The leg gained a preamble cap (8 KiB, derived like the node cap) and derives
its pointer set from exact quoted titles only; the runner's S1 rows now print
the node they pad and refuse to run on an OPEN one.

| row | defect | result | exit |
|---|---|---|---|
| N0 | CONTROL: no defect — the decision-map leg passes on the compacted tree | control — PASSED as designed | 0 |
| S1 | the LAST node (whichever `### ` is last — asserted RESOLVED by the row) padded to exactly one byte over the cap — the last-node span is measured without the file's final newline | CAUGHT | 1 |
| S1b | CONTROL: the LAST node padded to EXACTLY the cap — must PASS (an off-by-one would fail it) | control — PASSED as designed | 0 |
| P8 | plp-htmx's moved-narrative pointer rewritten UNQUOTED and naming plp-react-next (the phrase alone used to satisfy the both-directions leg) | CAUGHT | 1 |
| N9 | 20 KB of narrative pasted under `## Notes` (before the first `### `): the preamble cap | CAUGHT | 1 |

Across the four rounds: 34 rows — 23 caught, 10 controls passed as designed,
1 missed as stated in advance (N4).
