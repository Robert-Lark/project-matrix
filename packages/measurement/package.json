{
  "name": "@pm/measurement",
  "version": "0.1.0",
  "private": true,
  "description": "Shared measurement layer (ADR-0001). This slice: the versioned test-profile spec consumed by the HUD, bench runner, drift gate, and receipts. Dependency-free by design.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "types": "./src/index.ts",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.10"
  }
}
